version: '3'

tasks:
  deploy:server:
    desc: Deploy server to DEV (placeholder)
    cmds:
      - echo "Not yet configured -- add deploy config to infra/"

  deploy:frontend:
    desc: Deploy frontend to DEV (placeholder)
    cmds:
      - echo "Not yet configured"
